[project]
name = "data-engineering"
version = "0.1.0"
description = "Here apply ETL and use AWS EC2 and Airflow"
readme = "README.md"
authors = [
    { name = "EddyGiusepe", email = "eddychirinos.unac@gmail.com" }
]
requires-python = ">=3.13.7, <3.14"
dependencies = [
    "python-dotenv==1.2.3",
    "requests==2.34.2",
]

[dependency-groups]
dev = [
    "ruff>=0.16.4",
]

[tool.ruff]
extend = "ruff.toml"
